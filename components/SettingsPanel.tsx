
import React, { useState, useEffect } from 'react';
import { Download, Save, Lock } from 'lucide-react';
import { Business, BusinessSettings } from '../types';
import { CURRENCIES } from '../constants';
import { RichTextEditor } from './RichTextEditor';
import { api } from '../services/apiService';

interface SettingsPanelProps {
  business: Business;
  onUpdate: (business: Business) => void;
  onSave: () => void;
}

export const SettingsPanel: React.FC<SettingsPanelProps> = ({ business, onUpdate, onSave }) => {
    const [activeTab, setActiveTab] = useState('general');
    const [formData, setFormData] = useState<Business>(business);
    
    // Security State
    const [oldPassword, setOldPassword] = useState('');
    const [newPassword, setNewPassword] = useState('');
    const [securityMsg, setSecurityMsg] = useState({ type: '', text: '' });
    
    useEffect(() => { setFormData(business); }, [business]);
    
    const handleChange = (field: keyof Business, value: any) => { setFormData(prev => ({ ...prev, [field]: value })); };
    const handleSettingsChange = (field: keyof BusinessSettings, value: any) => { setFormData(prev => ({ ...prev, settings: { ...prev.settings, [field]: value } })); };
    const handleSaveSettings = () => { onUpdate(formData); onSave(); };
    
    const handleChangePassword = async () => {
        setSecurityMsg({ type: '', text: '' });
        if (!oldPassword || !newPassword) return setSecurityMsg({ type: 'error', text: 'All fields are required' });
        try {
            await api.changePassword(oldPassword, newPassword);
            setSecurityMsg({ type: 'success', text: 'Password changed successfully' });
            setOldPassword('');
            setNewPassword('');
        } catch (e: any) {
            setSecurityMsg({ type: 'error', text: e.message || 'Failed to change password' });
        }
    };

    const renderGeneral = () => (<div className="grid grid-cols-1 md:grid-cols-2 gap-6 animate-fade-in"><div className="md:col-span-2"><h3 className="text-lg font-bold border-b border-slate-200 dark:border-slate-700 pb-2 mb-4">Business Details</h3></div><div><label className="label-text">Business Name</label><input className="input-field" value={formData.name} onChange={e => handleChange('name', e.target.value)} /></div><div><label className="label-text">Email</label><input className="input-field" value={formData.email} onChange={e => handleChange('email', e.target.value)} /></div><div><label className="label-text">Phone</label><input className="input-field" value={formData.phone} onChange={e => handleChange('phone', e.target.value)} /></div><div><label className="label-text">TIN / Tax ID</label><input className="input-field" value={formData.settings.tin} onChange={e => handleSettingsChange('tin', e.target.value)} /></div><div className="md:col-span-2"><label className="label-text">Address</label><textarea className="input-field" rows={2} value={formData.address} onChange={e => handleChange('address', e.target.value)} /></div><div className="md:col-span-2 mt-4"><h3 className="text-lg font-bold border-b border-slate-200 dark:border-slate-700 pb-2 mb-4">Localization & Tax</h3></div><div><label className="label-text">Currency</label><select className="input-field" value={formData.settings.currency} onChange={e => handleSettingsChange('currency', e.target.value)}>{CURRENCIES.map(c => <option key={c} value={c}>{c}</option>)}</select></div><div><label className="label-text">Tax Label</label><input className="input-field" value={formData.settings.taxLabel} onChange={e => handleSettingsChange('taxLabel', e.target.value)} /></div><div><label className="label-text">Tax Rate (%)</label><input type="number" className="input-field" value={formData.settings.taxRate} onChange={e => handleSettingsChange('taxRate', Number(e.target.value))} /></div></div>);
    const renderBranding = () => (<div className="grid grid-cols-1 md:grid-cols-2 gap-6 animate-fade-in"><div className="md:col-span-2"><h3 className="text-lg font-bold border-b border-slate-200 dark:border-slate-700 pb-2 mb-4">Visual Identity</h3></div><div><label className="label-text">Primary Color</label><div className="flex gap-2"><input type="color" className="h-10 w-12 rounded cursor-pointer" value={formData.settings.primaryColor} onChange={e => handleSettingsChange('primaryColor', e.target.value)} /><input className="input-field" value={formData.settings.primaryColor} onChange={e => handleSettingsChange('primaryColor', e.target.value)} /></div></div><div><label className="label-text">Secondary Color</label><div className="flex gap-2"><input type="color" className="h-10 w-12 rounded cursor-pointer" value={formData.settings.secondaryColor} onChange={e => handleSettingsChange('secondaryColor', e.target.value)} /><input className="input-field" value={formData.settings.secondaryColor} onChange={e => handleSettingsChange('secondaryColor', e.target.value)} /></div></div><div className="md:col-span-2"><label className="label-text">Document Template</label><div className="grid grid-cols-2 md:grid-cols-4 gap-4 mt-2">{['classic', 'modern', 'minimal', 'professional'].map(t => (<div key={t} onClick={() => handleSettingsChange('template', t)} className={`border-2 rounded-lg p-4 cursor-pointer text-center capitalize ${formData.settings.template === t ? 'border-blue-600 bg-blue-50 dark:bg-blue-900/20' : 'border-slate-200 dark:border-slate-700 hover:border-slate-300'}`}>{t}</div>))}</div></div><div className="md:col-span-2 mt-4"><h3 className="text-lg font-bold border-b border-slate-200 dark:border-slate-700 pb-2 mb-4">Assets & URLs</h3></div><div className="md:col-span-2"><label className="label-text">Logo URL</label><input className="input-field" placeholder="https://..." value={formData.settings.logoUrl || ''} onChange={e => handleSettingsChange('logoUrl', e.target.value)} /></div><div><label className="label-text">Logo Alignment</label><select className="input-field" value={formData.settings.logoAlignment || 'left'} onChange={e => handleSettingsChange('logoAlignment', e.target.value)}><option value="left">Left</option><option value="center">Center</option><option value="right">Right</option></select></div><div className="md:col-span-2"><label className="label-text">Official Seal URL</label><input className="input-field" placeholder="https://..." value={formData.settings.sealUrl || ''} onChange={e => handleSettingsChange('sealUrl', e.target.value)} /></div><div><label className="label-text">Seal Alignment</label><select className="input-field" value={formData.settings.sealAlignment || 'right'} onChange={e => handleSettingsChange('sealAlignment', e.target.value)}><option value="left">Left</option><option value="center">Center</option><option value="right">Right</option></select></div><div className="md:col-span-2"><label className="label-text">Paid Stamp URL</label><input className="input-field" placeholder="https://..." value={formData.settings.paidStampUrl || ''} onChange={e => handleSettingsChange('paidStampUrl', e.target.value)} /></div></div>);
    const renderNumbering = () => (<div className="grid grid-cols-1 md:grid-cols-2 gap-6 animate-fade-in"><div className="md:col-span-2"><h3 className="text-lg font-bold border-b border-slate-200 dark:border-slate-700 pb-2 mb-4">Document Numbering</h3></div><div><label className="label-text">Invoice Prefix</label><input className="input-field" value={formData.settings.invoicePrefix} onChange={e => handleSettingsChange('invoicePrefix', e.target.value)} /></div><div><label className="label-text">Next Invoice #</label><input type="number" className="input-field" value={formData.settings.invoiceSequence} onChange={e => handleSettingsChange('invoiceSequence', Number(e.target.value))} /></div><div><label className="label-text">Quotation Prefix</label><input className="input-field" value={formData.settings.quotationPrefix} onChange={e => handleSettingsChange('quotationPrefix', e.target.value)} /></div><div><label className="label-text">Next Quote #</label><input type="number" className="input-field" value={formData.settings.quotationSequence} onChange={e => handleSettingsChange('quotationSequence', Number(e.target.value))} /></div><div><label className="label-text">Delivery Prefix</label><input className="input-field" value={formData.settings.deliveryNotePrefix} onChange={e => handleSettingsChange('deliveryNotePrefix', e.target.value)} /></div><div><label className="label-text">Next Delivery #</label><input type="number" className="input-field" value={formData.settings.deliveryNoteSequence} onChange={e => handleSettingsChange('deliveryNoteSequence', Number(e.target.value))} /></div><div><label className="label-text">Receipt Prefix</label><input className="input-field" value={formData.settings.paymentReceiptPrefix} onChange={e => handleSettingsChange('paymentReceiptPrefix', e.target.value)} /></div><div><label className="label-text">Next Receipt #</label><input type="number" className="input-field" value={formData.settings.paymentReceiptSequence} onChange={e => handleSettingsChange('paymentReceiptSequence', Number(e.target.value))} /></div></div>);
    const renderDefaults = () => (<div className="space-y-6 animate-fade-in"><div className="md:col-span-2"><h3 className="text-lg font-bold border-b border-slate-200 dark:border-slate-700 pb-2 mb-4">Default Notes</h3></div><div><RichTextEditor label="Default Invoice Notes" value={formData.settings.defaultInvoiceNotes || ''} onChange={val => handleSettingsChange('defaultInvoiceNotes', val)} /></div><div><RichTextEditor label="Default Quotation Notes" value={formData.settings.defaultQuotationNotes || ''} onChange={val => handleSettingsChange('defaultQuotationNotes', val)} /></div><div><RichTextEditor label="Default Delivery Note Notes" value={formData.settings.defaultDeliveryNoteNotes || ''} onChange={val => handleSettingsChange('defaultDeliveryNoteNotes', val)} /></div><div><RichTextEditor label="Default Payment Receipt Notes" value={formData.settings.defaultPaymentReceiptNotes || ''} onChange={val => handleSettingsChange('defaultPaymentReceiptNotes', val)} /></div><div className="md:col-span-2 mt-8"><h3 className="text-lg font-bold border-b border-slate-200 dark:border-slate-700 pb-2 mb-4">Default Terms & Conditions</h3></div><div><RichTextEditor label="Default Invoice Terms" value={formData.settings.defaultInvoiceTerms || ''} onChange={val => handleSettingsChange('defaultInvoiceTerms', val)} /></div><div><RichTextEditor label="Default Quotation Terms" value={formData.settings.defaultQuotationTerms || ''} onChange={val => handleSettingsChange('defaultQuotationTerms', val)} /></div><div><RichTextEditor label="Default Delivery Note Terms" value={formData.settings.defaultDeliveryNoteTerms || ''} onChange={val => handleSettingsChange('defaultDeliveryNoteTerms', val)} /></div><div><RichTextEditor label="Default Payment Receipt Terms" value={formData.settings.defaultPaymentReceiptTerms || ''} onChange={val => handleSettingsChange('defaultPaymentReceiptTerms', val)} /></div></div>);
    const renderSecurity = () => (
        <div className="space-y-6 animate-fade-in max-w-lg">
            <h3 className="text-lg font-bold border-b border-slate-200 dark:border-slate-700 pb-2 mb-4">Change Password</h3>
            {securityMsg.text && <div className={`p-3 text-sm rounded ${securityMsg.type === 'success' ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700'}`}>{securityMsg.text}</div>}
            <div><label className="label-text">Current Password</label><input type="password" className="input-field" value={oldPassword} onChange={e => setOldPassword(e.target.value)} /></div>
            <div><label className="label-text">New Password</label><input type="password" className="input-field" value={newPassword} onChange={e => setNewPassword(e.target.value)} /></div>
            <button onClick={handleChangePassword} className="bg-slate-800 text-white px-4 py-2 rounded hover:bg-slate-900 flex items-center gap-2"><Lock size={16}/> Update Password</button>
        </div>
    );
    const renderData = () => (<div className="space-y-6 animate-fade-in max-w-2xl"><div className="md:col-span-2"><h3 className="text-lg font-bold border-b border-slate-200 dark:border-slate-700 pb-2 mb-4">Data Management</h3></div><div className="p-4 border border-slate-200 dark:border-slate-700 rounded-lg bg-slate-50 dark:bg-slate-800"><h4 className="font-bold mb-2">Backup Data</h4><p className="text-sm text-slate-600 dark:text-slate-400 mb-4">Download a complete copy of your business data as a JSON file.</p><button onClick={() => {}} className="flex items-center gap-2 bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700"><Download size={16}/> Export Backup</button></div></div>);

    return (
        <div className="bg-white dark:bg-slate-800 rounded-xl shadow-sm border border-slate-100 dark:border-slate-700 h-full flex flex-col">
            <div className="p-6 border-b border-slate-100 dark:border-slate-700 flex justify-between items-center flex-shrink-0">
                <h2 className="text-2xl font-bold text-slate-800 dark:text-slate-100">Settings</h2>
                <div className="flex items-center gap-3">
                   <button onClick={handleSaveSettings} className="bg-blue-600 text-white px-6 py-2 rounded-lg font-bold hover:bg-blue-700 shadow-lg shadow-blue-600/20 flex items-center gap-2"><Save size={18}/> Save Changes</button>
                </div>
            </div>
            <div className="flex flex-col md:flex-row flex-1 overflow-hidden">
                <div className="w-full md:w-64 bg-slate-50 dark:bg-slate-900 border-r border-slate-200 dark:border-slate-800 overflow-y-auto">{['general', 'branding', 'documents', 'defaults', 'security', 'data'].map(tab => (<button key={tab} onClick={() => setActiveTab(tab)} className={`w-full text-start px-6 py-4 font-medium flex items-center gap-3 border-l-4 transition-colors ${activeTab === tab ? 'bg-white dark:bg-slate-800 border-blue-600 text-blue-600 dark:text-blue-400' : 'border-transparent text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800'}`}><span className="capitalize">{tab}</span></button>))}</div>
                <div className="flex-1 overflow-y-auto p-6 md:p-8">{activeTab === 'general' && renderGeneral()}{activeTab === 'branding' && renderBranding()}{activeTab === 'documents' && renderNumbering()}{activeTab === 'defaults' && renderDefaults()}{activeTab === 'security' && renderSecurity()}{activeTab === 'data' && renderData()}</div>
            </div>
        </div>
    );
};
